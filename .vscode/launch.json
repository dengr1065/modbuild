{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Live Server",
            "program": "${workspaceFolder}/modbuild.js"
        }
    ]
}